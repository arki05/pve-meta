#!/bin/sh
# postrm for the pve-meta package.
#
# The actual patch reversal (`pve-ext-patch remove pve-meta-lifecycle`,
# restoring the seven lifecycle files' pristine content) happens in
# debian/prerm's `remove` case, *before* this package's own files are
# deleted. This script exists for documentation and to host debhelper's
# own generated snippet below.
set -e

#DEBHELPER#

exit 0
