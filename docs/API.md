# pve-meta HTTP API

Superseded by [`DESIGN.md`](DESIGN.md), section 5 ("API (native, `/api2/json/meta`,
served by pveproxy/pvedaemon)"), which is the single source of truth for the API:
the endpoint table (methods, paths, parameters, return shapes), the documents-on-the-wire
rules (section 4), the view/scopes access model (sections 2 and 3), and the
implementation split between `PVE::RS::Meta` (`crates/pve-meta-perl`) and
`perl/PVE/API2/Ext/Meta.pm`.

This file is kept only as a redirect for old links; do not add content here.
